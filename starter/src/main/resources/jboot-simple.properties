# Cookie's encryption key (recommended for more than 32 or more)
jboot.web.cookieEncryptKey = 6shudder7propagate4gunwale!rifleman:

# JWT key, for API communication such as applet (recommended for more than 32 digits)
jboot.web.jwt.secret = 9titlist$AMBULANT7heuristics1pilferer?

# Production environment configuration
#   1) No output debug Log
#   2) Caches the template to improve performance (modify HTML through the file and not refresh in real time)
#   3) Turn off the production mode and put it product change into dev
jboot.app.mode = product


# Database -related configuration
jboot.datasource.type=mysql
jboot.datasource.url=jdbc:mysql://127.0.0.1:3306/jpress?useUnicode=true&useSSL=false&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull
jboot.datasource.user=root
jboot.datasource.password=
